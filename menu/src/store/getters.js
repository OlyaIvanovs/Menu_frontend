const getters = {
    ategories(state) {
        return state.ategories
    },

    categories(state) {
        return state.categories
    },

    recipes(state) {
        return state.recipes
    }
}

export default getters